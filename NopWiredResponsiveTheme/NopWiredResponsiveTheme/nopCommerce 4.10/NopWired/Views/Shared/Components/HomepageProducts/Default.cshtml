@model IList<ProductOverviewModel>

@if (Model.Count > 0)
{
    <div class="owl-carousel home-page-product-grid">
        @foreach (var item in Model)
        {
            <div>
                @await Html.PartialAsync("_HomeProductBox", item)
            </div>
        }
    </div>
}

